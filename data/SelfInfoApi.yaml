check_login:
  method: get
  url: ${host}/cellphone/existence/check
  params:
    phone: ${phone}
  cookies:
  data:

get_info:
  method: get
  url: ${host}/user/account
  cookies:
  params:
  data:

update_info:
  method: get
  url: ${host}/user/account
  params:
    gender: ${gender}
    signature: ${signature}
    city: ${city}
    nickname: ${nickname}
    birthday: ${birthday}
    province: ${province}
  cookies:
  data:

# 1525918298004
# 440000